- name: install nginx in another server
  hosts: web
  become: yes
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: present
    - name:  Remove the nginx package
      ansible.builtin.package:
        name: nginx
        state: absent